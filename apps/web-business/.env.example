# apps/mobile/.env — local dev config for Expo (gitignored).
# Last write wins, so keep one entry per variable.

# Backend API on the GCP VM (HTTP, no domain yet).
# To test against your laptop instead, comment this out and set:
#   EXPO_PUBLIC_EVENTFLOW_API_URL=http://<your-mac-LAN-IP>:8000

# Supabase (auth). These are also in .env.local — Expo merges both.
EXPO_PUBLIC_SUPABASE_URL=https://enlxdeiitvezfrubhhfv.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_UJHVZXCREQLcuLhzzM5v_A__2-Fb0di

# Maps key for the native Android MapView (baked at build time via app.config.ts).
GOOGLE_MAPS_API_KEY=
EXPO_ACCESS_TOKEN=8nGW16YLe85pXWPPMwyOWbkf_zoPdp6Or5HpvHu9
